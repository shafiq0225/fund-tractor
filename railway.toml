[build]
builder = "DOCKERFILE"

[deploy]
numReplicas = 1

[[services]]
name = "web"
sourcePath = "."
type = "web"

[services.ports]
http = "8080"

[services.healthcheck]
httpPath = "/health"
initialDelay = 10
period = 30
timeout = 10